# Terminal Rebuild Tasks

- [x] Add `list` command for recent runs in the terminal
- [ ] Add `export` command for packaging a run's transcript and artifacts
- [ ] Add provider preflight checks before `run` and `resume`
- [ ] Add CLI-side guardrails for `confirm` approval mode
