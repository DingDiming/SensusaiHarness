# Legacy Reference

The directories under `legacy/` are the previous Web-first implementation.

They are kept only as reference assets for:

- runtime and artifact semantics
- schema and persistence ideas
- prompt and orchestration patterns worth porting

They are not part of the current product runtime and should not receive new feature work unless the migration plan explicitly calls for it.
